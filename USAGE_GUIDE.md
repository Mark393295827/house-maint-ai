# House Maint AI — 产品使用说明手册

**文档版本：** v1.0.0  
**适用版本：** House Maint AI 1.0  
**最后更新：** 2026-04-10  
**机密等级：** 内部使用

---

## 目录

1. [产品概述](#1-产品概述)
2. [系统架构与技术栈](#2-系统架构与技术栈)
3. [环境部署与启动](#3-环境部署与启动)
4. [账号体系与权限管理](#4-账号体系与权限管理)
5. [功能模块详解](#5-功能模块详解)
   - 5.1 [住户端功能](#51-住户端功能)
   - 5.2 [师傅端功能](#52-师傅端功能)
   - 5.3 [企业管控台](#53-企业管控台-adminmanager-专属)
6. [核心业务流程](#6-核心业务流程)
7. [工单全生命周期](#7-工单全生命周期)
8. [API 接口规范](#8-api-接口规范)
9. [常见问题与排查](#9-常见问题与排查)
10. [运维与数据库管理](#10-运维与数据库管理)

---

## 1. 产品概述

House Maint AI 是一款面向中国城市住宅市场的 **B2B2C AI 驱动维修派单平台**，核心定位为物业管理公司、企业园区与住宅社区的数字化运维工具。

### 1.1 产品定位

平台围绕"报修即派单"的核心价值主张，通过 AI 图像/语音识别自动完成故障诊断、工种匹配、师傅调度三步核心链路，将传统报修响应时间从数小时压缩至 **30 秒内**。

### 1.2 用户角色体系

| 角色层级 | 角色标识 | 核心职能 |
|----------|----------|----------|
| 平台管理员 | `admin` | 全平台数据查看与操作，管控 AI Agent 状态 |
| 物业经理 | `manager` | 工单总览、工人绩效管理、数据分析 |
| 企业租户 | `tenant` | 提交工单、资产管理、查看进度 |
| 普通住户 | `user` | AI 诊断报修、查看工单进度、评价服务 |
| 维修师傅 | `worker` | 接单、查看任务、标记完工 |

### 1.3 核心能力矩阵

| 能力模块 | 技术实现 | 业务价值 |
|----------|----------|----------|
| AI 故障诊断 | Gemini Vision + DeepSeek | 替代人工初步判断，减少无效上门 |
| 智能派单匹配 | 多维评分算法（距离 + 技能 + 评分） | 最优师傅自动匹配，提升首次修复率 |
| 实时状态推送 | WebSocket + 通知中心 | 全程透明化，降低客诉率 |
| 企业运营看板 | 多维度 KPI 聚合 | 管理者实时掌握运营健康度 |
| 全渠道接入 | 微信 / 网页 / API | 零额外 App 安装，降低用户门槛 |

---

## 2. 系统架构与技术栈

### 2.1 技术栈

| 层级 | 技术选型 | 版本 |
|------|----------|------|
| 前端框架 | React | 19 |
| 前端语言 | TypeScript | 5.x |
| 前端构建 | Vite | 6.x |
| 样式系统 | Tailwind CSS | 3.x |
| 后端框架 | Express.js (Node.js) | 20+ |
| 数据库（开发） | SQLite | — |
| 数据库（生产） | PostgreSQL | 15+ |
| 实时通信 | Socket.IO | — |
| 身份认证 | JWT（HttpOnly Cookie） | — |
| AI 引擎 | Gemini Vision / DeepSeek | — |
| 安全防护 | Helmet / HPP / CSRF Guard / Rate Limiter | — |

### 2.2 服务端口

| 服务 | 本地地址 | 说明 |
|------|----------|------|
| 前端开发服务器 | `http://localhost:5173` | Vite Dev Server |
| 后端 API 服务 | `http://localhost:3001` | Express REST API |

---

## 3. 环境部署与启动

### 3.1 前置依赖

- Node.js ≥ 20.x
- npm ≥ 10.x（或 pnpm）

### 3.2 依赖安装

```bash
# 安装前端依赖
npm install

# 安装后端依赖
cd server && npm install && cd ..
```

### 3.3 环境变量配置

复制示例文件并按需填写：

```bash
cp .env.example .env
cp server/.env.example server/.env   # 如存在
```

关键环境变量说明：

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `PORT` | 后端监听端口 | `3001` |
| `JWT_SECRET` | JWT 签名密钥（生产环境必须修改） | — |
| `DATABASE_URL` | PostgreSQL 连接串（生产） | — |
| `GEMINI_API_KEY` | Google Gemini AI 密钥 | — |
| `CORS_ORIGINS` | 允许跨域的前端域名（逗号分隔） | — |

### 3.4 数据库初始化（首次部署必须执行）

```bash
cd server && npx tsx scripts/init-db.ts
```

> 该脚本将创建全部数据表并写入测试账号数据。执行成功后输出 `✅ Database initialization complete!`

### 3.5 启动服务

#### 推荐方式：一键启动前后端

```bash
npm run dev:all
```

#### 分离启动

```bash
# 仅启动前端
npm run dev

# 仅启动后端
npm run dev:server
```

#### 生产构建

```bash
npm run build        # 构建前端静态资源
cd server && npm run build  # 编译后端 TypeScript
```

---

## 4. 账号体系与权限管理

### 4.1 测试账号（开发/演示环境）

> **所有测试账号统一密码：`123456`**

#### 管理类账号

| 角色 | 手机号 | 姓名 | 权限范围 |
|------|--------|------|----------|
| 平台管理员（admin） | `13800138000` | 管理员 | 全平台所有功能，含企业后台、指标看板、工人管理 |
| 物业经理（manager） | `13800138005` | 物业经理 | 企业后台、数据分析、工单总览（不含系统配置） |

#### 住户类账号

| 角色 | 手机号 | 姓名 | 权限范围 |
|------|--------|------|----------|
| 普通住户（user） | `13800138001` | Alex 用户 | AI 诊断、工单提交与查看、师傅匹配、评价 |
| 企业租户（tenant） | `13800138006` | 企业租户 | AI 诊断、工单提交与查看、资产管理 |

#### 维修师傅账号

| 角色 | 手机号 | 姓名 | 技能标签 |
|------|--------|------|----------|
| 师傅（worker） | `13800138002` | Wang Shifu | 水工（plumbing）、电工（electrical） |
| 师傅（worker） | `13800138003` | Li Shifu | 水工（plumbing） |
| 师傅（worker） | `13800138004` | Zhang Shifu | 电工（electrical）、家电（appliance） |

> **注意：** 师傅账号须通过专属入口登录：`http://localhost:5173/repairman/login`

### 4.2 权限矩阵

| 功能模块 | admin | manager | user | tenant | worker |
|----------|:-----:|:-------:|:----:|:------:|:------:|
| AI 诊断报修 | ✓ | ✓ | ✓ | ✓ | — |
| 查看我的工单 | ✓ | ✓ | ✓ | ✓ | — |
| 资产管理 | ✓ | ✓ | ✓ | ✓ | — |
| 评价工单 | ✓ | ✓ | ✓ | ✓ | — |
| 师傅工作台 | ✓ | — | — | — | ✓ |
| 接单 / 完工操作 | ✓ | — | — | — | ✓ |
| 企业管控台 | ✓ | ✓ | — | — | — |
| 运营指标看板 | ✓ | ✓ | — | — | — |
| 全平台工单管理 | ✓ | ✓ | — | — | — |

### 4.3 身份认证机制

系统采用 **JWT + HttpOnly Cookie** 双令牌认证方案：

- **Access Token**：短生命周期，存储于 `accessToken` Cookie（HttpOnly，防 XSS）
- **Refresh Token**：长生命周期，用于无感刷新 Access Token
- **CSRF 防护**：所有写操作（POST/PUT/DELETE）强制验证 CSRF Token
- **登录失效**：会话过期后自动跳转至 `/login`，并保留原访问路径供登录后恢复

---

## 5. 功能模块详解

### 5.1 住户端功能

#### 5.1.1 AI 智能诊断（`/diagnosis`）

**适用角色：** user、tenant、admin、manager

AI 诊断是平台核心功能，支持以下输入方式：

| 输入类型 | 说明 |
|----------|------|
| 图片上传 | 支持 JPG/PNG，AI 通过 Gemini Vision 识别故障类型与损坏程度 |
| 语音描述 | 录制语音，AI 转录并分析故障信息 |
| 文字描述 | 手动填写标题与详细描述 |

诊断完成后系统自动生成工单并触发师傅匹配流程。

**工单分类（category）：**

| 分类标识 | 中文名称 |
|----------|----------|
| `plumbing` | 水工 / 管道 |
| `electrical` | 电工 / 电路 |
| `appliance` | 家电维修 |
| `carpentry` | 木工 / 家具 |
| `painting` | 墙面 / 油漆 |
| `other` | 其他 |

#### 5.1.2 我的工单（`/cases`）

展示当前用户提交的全部工单列表，支持按状态筛选。每条工单展示：工单标题、分类、状态标签、创建时间、匹配师傅信息。

#### 5.1.3 师傅匹配（`/worker/match`）

展示系统为当前工单推荐的最优师傅候选列表，排序依据为综合评分（距离权重 + 技能匹配度 + 历史评分）。

#### 5.1.4 资产管理（`/assets`）

管理房屋内设备台账，记录设备名称、型号、安装日期、最近维修记录，用于辅助 AI 诊断与历史追溯。

#### 5.1.5 工单评价（`/review/:id`）

工单状态变为 `completed` 后，住户可对师傅服务进行 1-5 星评价并填写文字点评。评价数据将影响师傅综合评分与派单优先级。

---

### 5.2 师傅端功能

#### 5.2.1 登录入口

师傅专属登录页面：`http://localhost:5173/repairman/login`

> 普通住户登录页（`/login`）不适用于师傅账号，请勿混用。

#### 5.2.2 师傅工作台（`/worker/dashboard`）

**适用角色：** worker、admin

工作台首页展示以下数据面板：

| 数据卡片 | 说明 |
|----------|------|
| 累计收入 | 已完成工单对应订单金额汇总 |
| 完成工单数 | 历史完工总数 |
| 进行中任务 | 当前状态为 `matching / matched / in_progress` 的工单数 |
| 综合评分 | 基于住户评价的加权平均分 |

工作台同时展示**待接订单推送列表**，师傅可在此查看附近工单详情并一键接单。

#### 5.2.3 工单详情（`/worker/job/:id`）

展示单条工单完整信息：
- 故障描述与分类
- 客户联系方式
- AI 诊断结果与建议方案
- 完工操作入口（填写解决步骤、用料记录、费用说明）

#### 5.2.4 师傅注册（`/worker/register`）

新师傅首次使用需完成资质注册，填写内容包括：
- 技能标签（可多选）
- 当前定位（用于距离匹配）
- 个人简介与收费标准（可选）

---

### 5.3 企业管控台（admin / manager 专属）

入口：`http://localhost:5173/enterprise`

#### 5.3.1 总览仪表盘（`/enterprise`）

| 功能区块 | 说明 |
|----------|------|
| 运营评分环形图 | 多维度（响应速度、完工率、用户满意度等）健康度评分 |
| AI Agent 状态监控 | 各 AI Agent 在线状态、今日调用次数、运行成本 |
| 策略告警列表 | 触发预设阈值的业务指标告警，含建议处置动作 |
| 实时地图 | 师傅分布与工单热力图 |
| 绩效趋势图 | 近期完工率、响应时长折线图 |

#### 5.3.2 子模块导航

| 路径 | 功能 |
|------|------|
| `/enterprise/properties` | 物业房源管理 |
| `/enterprise/tickets` | 全平台工单看板（可筛选、搜索） |
| `/enterprise/workers` | 师傅资质审核与绩效排名 |
| `/enterprise/analytics` | 业务数据分析与报表导出 |

#### 5.3.3 运营指标看板（`/metrics`）

**适用角色：** admin、manager

展示系统级 KPI 指标，包括：API 响应延迟、请求成功率、AI 诊断准确率、派单成功率、平均响应时长等实时与历史数据。

---

## 6. 核心业务流程

### 6.1 住户报修全流程

```
① 住户登录
        ↓
② 进入 /diagnosis → 上传照片或描述故障
        ↓
③ AI 分析（Gemini Vision）→ 生成故障报告与工种建议
        ↓
④ 系统创建工单（状态：pending）
        ↓
⑤ 智能匹配引擎运行（状态：matching → broadcasted）
        ↓
⑥ 师傅接单（状态：matched）
        ↓
⑦ 师傅上门维修（状态：in_progress）
        ↓
⑧ 师傅标记完工（状态：completed）
        ↓
⑨ 住户收到通知 → 进入 /review/:id 提交评价
```

### 6.2 师傅接单流程

```
① 师傅通过 /repairman/login 登录
        ↓
② 进入 /worker/dashboard → 查看推送工单列表
        ↓
③ 查看工单详情 → 确认可接 → 点击「接单」
        ↓
④ 前往客户现场完成维修
        ↓
⑤ 在 /worker/job/:id → 填写完工信息（步骤、用料、费用）
        ↓
⑥ 提交完工 → 工单状态更新为 completed
        ↓
⑦ 师傅完工数 +1，收入自动汇总至工作台
```

### 6.3 管理员监控流程

```
① admin/manager 登录
        ↓
② 进入 /enterprise → 查看运营健康评分与 AI Agent 状态
        ↓
③ 查看策略告警 → 按建议处置（可一键审批或忽略）
        ↓
④ 进入 /enterprise/analytics → 下钻分析问题维度
        ↓
⑤ 进入 /metrics → 核查系统级性能指标
```

---

## 7. 工单全生命周期

### 7.1 状态流转图

```
pending
   │
   ▼
matching ──────────────────────► failed_analysis
   │
   ▼
broadcasted
   │
   ▼
matched ───────────────────────► flagged_for_review
   │
   ▼
in_progress
   │
   ▼
completed
   
（任意阶段可由管理员手动取消）
   └──────────────────────────► cancelled
```

### 7.2 状态说明

| 状态标识 | 中文说明 | 触发方式 |
|----------|----------|----------|
| `pending` | 已提交，等待 AI 分析 | 住户提交工单后自动进入 |
| `matching` | AI 正在处理与匹配 | 系统 AI 引擎启动后变更 |
| `broadcasted` | 已广播至附近师傅 | 匹配算法运行完毕后推送 |
| `matched` | 师傅已接单，待上门 | 师傅点击接单后变更 |
| `in_progress` | 维修进行中 | 师傅到场确认后变更 |
| `completed` | 已完工 | 师傅提交完工信息后变更 |
| `cancelled` | 已取消 | 住户主动取消或管理员操作 |
| `failed_analysis` | AI 分析失败 | AI 引擎异常时自动标记 |
| `flagged_for_review` | 已标记，待人工审核 | 系统检测到异常工单时自动触发 |

---

## 8. API 接口规范

### 8.1 基础说明

- **Base URL：** `http://localhost:3001/api`
- **认证方式：** HttpOnly Cookie（`accessToken`），登录后由服务端自动写入
- **数据格式：** 请求与响应均为 `application/json`
- **统一响应结构：**

```json
{
  "success": true,
  "data": { ... },
  "error": null,
  "meta": {
    "total": 100,
    "page": 1,
    "limit": 20
  }
}
```

### 8.2 核心接口速查

| 接口 | 方法 | 说明 | 权限要求 |
|------|------|------|----------|
| `/auth/login` | POST | 用户登录 | 公开 |
| `/auth/logout` | POST | 退出登录 | 已登录 |
| `/auth/me` | GET | 获取当前用户信息 | 已登录 |
| `/reports` | POST | 创建工单 | 已登录 |
| `/reports` | GET | 获取工单列表 | 已登录 |
| `/reports/:id` | GET | 获取单条工单详情 | 已登录 |
| `/reports/:id/complete` | PUT | 标记工单完工 | worker / admin |
| `/reports/available` | GET | 获取可接工单列表 | worker |
| `/worker-portal/dashboard` | GET | 师傅工作台数据 | worker |
| `/worker-portal/jobs` | GET | 师傅已接工单列表 | worker |
| `/reviews` | POST | 提交工单评价 | 已登录 |
| `/analytics/summary` | GET | 运营数据汇总 | admin / manager |
| `/metrics` | GET | 系统性能指标 | admin / manager |

### 8.3 请求速率限制

| 限制类型 | 规则 | 适用接口 |
|----------|------|----------|
| 标准限流 | 100次 / 15分钟 / IP | 大部分接口 |
| 严格限流 | 10次 / 15分钟 / IP | 登录、注册等认证接口 |
| 用户级限流 | 按 user_id 独立计数 | 工单创建、AI 诊断 |

---

## 9. 常见问题与排查

### Q1：访问 `/enterprise` 被重定向，无法进入

**原因：** 当前登录账号角色不是 `admin` 或 `manager`。

**解决：** 退出后使用以下账号重新登录：
- 管理员：`13800138000` / `123456`
- 物业经理：`13800138005` / `123456`

---

### Q2：数据库报错，页面数据为空

**原因：** 数据库未初始化或 `sqlite.db` 文件损坏。

**解决：**
```bash
# 删除旧数据库文件（如存在）
rm sqlite.db server/sqlite.db

# 重新初始化
cd server && npx tsx scripts/init-db.ts
```

---

### Q3：师傅登录后无法接单，提示"未找到工人资料"

**原因：** 该师傅账号未创建 worker profile。

**解决：** 登录后进入 `/worker/register` 完成资质注册，填写技能与定位信息。

---

### Q4：前端启动正常但 API 请求失败（Network Error）

**原因：** 后端服务未启动，或端口冲突。

**排查步骤：**
```bash
# 检查后端是否运行
curl http://localhost:3001/api/auth/me

# 如未运行，启动后端
npm run dev:server
```

---

### Q5：工单状态长期停留在 `matching`，不推进

**原因：** AI 分析服务异常（API Key 未配置或余额不足）。

**排查：** 检查 `.env` 中 `GEMINI_API_KEY` 是否正确配置，查看后端日志中的 AI 调用错误。

---

## 10. 运维与数据库管理

### 10.1 数据库文件

| 环境 | 数据库类型 | 文件位置 |
|------|----------|----------|
| 开发 | SQLite | `./sqlite.db`（项目根目录） |
| 生产 | PostgreSQL | 由 `DATABASE_URL` 环境变量指定 |

### 10.2 常用运维命令

```bash
# 查看用户信息（示例：查询手机号 13800138000）
cd server && npx tsx scripts/check-user.ts

# 重置并重新初始化数据库
rm sqlite.db && npx tsx scripts/init-db.ts

# 运行单元测试
npm test

# 查看测试覆盖率报告
npm run test:coverage

# 代码规范检查
npm run lint
```

### 10.3 日志文件

| 日志文件 | 内容 |
|----------|------|
| `server/be_startup.log` | 后端启动日志 |
| `server/server_crash.log` | 后端崩溃日志 |
| `server/server_start.log` | 服务启动状态记录 |

### 10.4 安全注意事项

- **生产环境部署前，必须修改 `JWT_SECRET`**，禁止使用默认值
- 所有测试账号密码（`123456`）**仅限开发/演示环境使用**，上线前须全部删除或重置
- 数据库文件 `sqlite.db` 包含用户数据，**禁止提交至版本控制系统**（已在 `.gitignore` 中忽略）
- 生产环境须配置 HTTPS，Cookie 的 `Secure` 属性由服务端自动启用

---

*本文档由工程团队维护，如有疑问请联系项目负责人或提交 Issue。*
